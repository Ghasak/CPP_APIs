#include "moduleA.cpp"
/* delcare my current function */
float addFunction(float& a , float& b);
/* [declartion] This is a reference function, passing the alias of the variable */
void myFirstFunction( int& var, int& idx);
/* [declartion] Passing a memeory address and store in a pointer */
void myPointerFunction(int* var, int* idx);
void myFunctionM(int& val);

